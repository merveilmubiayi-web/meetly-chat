import { registerRootComponent } from 'expo';
import App from './App'; // Assure-toi que ton App.js est bien à la racine

// C'est ici qu'on enregistre proprement le composant principal
registerRootComponent(App);